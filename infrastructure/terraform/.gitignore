# Terraform state (contains secrets)
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# Variables file (contains secrets)
terraform.tfvars

# Worker bundle (built from source)
worker-bundle.js

# Terraform state (contains secrets)
*.tfstate
*.tfstate.backup
.terraform/
# .terraform.lock.hcl is committed — ensures consistent provider versions

# Variables file (contains secrets)
terraform.tfvars

# Environment files with real secrets (manual deploy)
.env.local

# Worker bundle (built from source)
worker-bundle.js
